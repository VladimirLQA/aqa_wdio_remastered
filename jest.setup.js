// Jest setup file for API tests
// Configure environment and global settings for Jest tests

// Set test timeout
// jest.setTimeout(30000); // This is now handled in jest.config.js

// Global test setup
beforeAll(() => {
  // Global setup logic if needed
});

afterAll(() => {
  // Global cleanup logic if needed
});