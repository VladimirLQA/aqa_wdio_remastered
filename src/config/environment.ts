import 'dotenv/config';

export const ADMIN_USERNAME = `${process.env.ADMIN_USERNAME}`;
export const ADMIN_PASSWORD = `${process.env.ADMIN_PASSWORD}`;
export const SALES_PORTAL_URL = `${process.env.SALES_PORTAL_URL}`;
export const BAIL = `${process.env.BAIL}`;
export const HEADLESS = `${process.env.HEADLESS}`;
export const MAX_INSTANCES = `${process.env.MAX_INSTANCES}`;
export const TEST_NAME_PATTERN = `${process.env.TEST_NAME_PATTERN}`;
export const FRAMEWORK = `${process.env.FRAMEWORK}`;
